apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-dante-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "helm-dante-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-dante-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
