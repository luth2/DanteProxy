apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dante-proxy.fullname" . }}
  labels:
    {{- include "helm-dante-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dante-proxy.selectorLabels" . | nindent 4 }}
