apiVersion: v1
kind: Service
metadata:
  name: {{ include "DanteProxy.fullname" . }}
  labels:
    {{- include "DanteProxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "DanteProxy.selectorLabels" . | nindent 4 }}
